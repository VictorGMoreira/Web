<!DOCTYPE html>
<html lang="pt-BR">
  <%- include('partials/head') %>

  <body class="bg-black text-white">

    <!-- Header -->
    <%- include('partials/header') %>

    <div class="container mx-auto p-4">
      <h2 class="text-3xl font-bold mt-2"><%= series.title %></h2>
      <p class="text-gray-400">Avaliação: <%= series.rating %> · <%= series.releaseYear %> · <%= series.genre %></p>

      <div class="movie-detail flex flex-col md:flex-row">
        <img src="<%= series.image %>" alt="Poster de <%= series.title %>" class="w-48 h-72 rounded-md mb-4 md:mr-4" />
        
        <div class="flex flex-col">
          <p class="text-gray-400 mb-4"><%= series.overview %></p>
          <p><strong>Temporadas:</strong> <%= series.seasons %></p>
          <p><strong>Episódios:</strong> <%= series.episodes %></p>
          <form action="/salvarSerie" method="POST">
            <input type="hidden" name="seriesId" value="<%= series.id %>">
            <input type="hidden" name="title" value="<%= series.title %>">
            <input type="hidden" name="image" value="<%= series.image %>">
            <input type="hidden" name="overview" value="<%= series.overview %>">
            <button type="submit" class="text-2xl text-white hover:text-blue-500">
              <i class="fas fa-save"></i> Salvar
            </button>
          </form>
        </div>
      </div>

      <!-- Ícone de Salvar (canto superior direito) -->
      
        

    </div>

    <!-- Footer -->
    <%- include('partials/footer') %>

  </body>
</html>
